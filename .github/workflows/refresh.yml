# Cloud auto-refresh: scan Gmail + rescore + republish on a schedule (and on
# demand), so the published dashboard stays fresh without your PC being on. The
# mail scan runs here on GitHub's runners rather than localhost, so it needs a few
# repository Secrets -- see the SETUP block at the bottom of this file.
#
# Privacy model: a cloud-safe SQLite copy (jobs, monitors, reviews, mail, and
# applications) is kept AES-256-GCM encrypted on the private `data` branch. Local
# campaign tables are emptied and vacuumed from that copy before encryption. The
# latest and one last-known-good encrypted blob are stored, and both are useless
# without JOBSCOPE_DB_KEY. Pages receives an empty public shell plus the
# passphrase-encrypted whole-site payload. AI is left off here.

name: refresh
run-name: ${{ inputs.mutation_nonce && format('Monitoring sync {0}', inputs.mutation_nonce) || format('Refresh ({0})', github.event_name) }}

on:
  schedule:
    - cron: "17 */3 * * *"    # every 3 hours (UTC)
  workflow_dispatch:           # manual "Run workflow" button -- works from the GitHub mobile app
    inputs:
      full_scan:
        description: "Also scan job boards (slower)"
        type: boolean
        default: false
      inbox_backfill:
        description: "Re-scan ALL inbox history and re-score (one-time heal; slower)"
        type: boolean
        default: false
      mutations_json:
        description: "Validated company/review action batch (normally sent by the encrypted dashboard)"
        type: string
        default: ""
      mutation_nonce:
        description: "Internal browser correlation token"
        type: string
        default: ""

concurrency:
  group: refresh
  cancel-in-progress: false

permissions:
  contents: write              # push the encrypted DB to `data` and the site to `gh-pages`

jobs:
  refresh:
    runs-on: ubuntu-latest
    steps:
      - name: Check out code (main)
        uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7

      - name: Guard -- required secrets present
        id: guard
        env:
          JOBSCOPE_CONFIG: ${{ secrets.JOBSCOPE_CONFIG }}
          JOBSCOPE_DB_KEY: ${{ secrets.JOBSCOPE_DB_KEY }}
          JOBSCOPE_GMAIL_APP_PW: ${{ secrets.JOBSCOPE_GMAIL_APP_PW }}
          JOBSCOPE_APPS_PASSPHRASE: ${{ secrets.JOBSCOPE_APPS_PASSPHRASE }}
        run: |
          if [ -z "$JOBSCOPE_CONFIG" ] || [ -z "$JOBSCOPE_DB_KEY" ] || [ -z "$JOBSCOPE_GMAIL_APP_PW" ] || [ -z "$JOBSCOPE_APPS_PASSPHRASE" ]; then
            echo "::notice::Cloud refresh is not configured yet (missing secrets) -- skipping. See the SETUP block in .github/workflows/refresh.yml."
            echo "ready=false" >> "$GITHUB_OUTPUT"
          else
            echo "ready=true" >> "$GITHUB_OUTPUT"
          fi

      - name: Set up Python
        if: steps.guard.outputs.ready == 'true'
        uses: actions/setup-python@ece7cb06caefa5fff74198d8649806c4678c61a1 # v6
        with:
          python-version: "3.12"

      - name: Set up Node
        if: steps.guard.outputs.ready == 'true'
        uses: actions/setup-node@249970729cb0ef3589644e2896645e5dc5ba9c38 # v6
        with:
          node-version: "20"

      - name: Install Python deps
        if: steps.guard.outputs.ready == 'true'
        run: python -m pip install --requirement requirements.lock

      - name: Write config (from secret -- contains no passwords)
        if: steps.guard.outputs.ready == 'true'
        env:
          JOBSCOPE_CONFIG: ${{ secrets.JOBSCOPE_CONFIG }}
        run: |
          mkdir -p data
          printf '%s' "$JOBSCOPE_CONFIG" > config.yaml

      - name: Restore encrypted DB from the `data` branch
        id: restore
        if: steps.guard.outputs.ready == 'true'
        env:
          JOBSCOPE_DB_KEY: ${{ secrets.JOBSCOPE_DB_KEY }}
        run: |
          set -euo pipefail
          data_ref="$(git ls-remote --heads origin refs/heads/data)"
          if [ -z "$data_ref" ]; then
            echo "::error::No data branch exists. Seed it with scripts/seed-cloud-db.ps1 before running cloud refresh."
            exit 1
          fi

          git fetch --no-tags --depth 1 origin refs/heads/data
          data_sha="$(git rev-parse FETCH_HEAD)"
          git show "$data_sha:jobscope.db.enc" > data/jobscope.db.enc 2>/dev/null || rm -f data/jobscope.db.enc
          git show "$data_sha:jobscope.db.previous.enc" > data/jobscope.db.previous.enc 2>/dev/null || rm -f data/jobscope.db.previous.enc

          restore_snapshot() {
            encrypted_path="$1"
            rm -f data/jobscope.db
            [ -s "$encrypted_path" ] || return 1
            node scripts/crypt-file.mjs decrypt "$encrypted_path" data/jobscope.db || return 1
            python -m jobscope.core.snapshot data/jobscope.db || return 1
            cp "$encrypted_path" data/jobscope.db.restored.enc
          }

          if restore_snapshot data/jobscope.db.enc; then
            echo "restored current DB snapshot from data@$data_sha"
          elif restore_snapshot data/jobscope.db.previous.enc; then
            echo "::warning::Current DB snapshot is invalid; restored the last-known-good generation from data@$data_sha"
          else
            rm -f data/jobscope.db
            echo "::error::Neither encrypted DB generation restored as a valid Jobscope database. Refusing to create a replacement database."
            exit 1
          fi

          echo "data_sha=$data_sha" >> "$GITHUB_OUTPUT"

      - name: Sync inbox + rescore (AI off)
        if: steps.guard.outputs.ready == 'true'
        env:
          PYTHONPATH: "."
          JOBSCOPE_GMAIL_APP_PW: ${{ secrets.JOBSCOPE_GMAIL_APP_PW }}
          JOBSCOPE_GMAIL_APP_PW_2: ${{ secrets.JOBSCOPE_GMAIL_APP_PW_2 }}
          JOBSCOPE_SMTP_PASSWORD: ${{ secrets.JOBSCOPE_SMTP_PASSWORD }}
          JOBSCOPE_HUNTER_API_KEY: ${{ secrets.JOBSCOPE_HUNTER_API_KEY }}
          JOBSCOPE_APOLLO_API_KEY: ${{ secrets.JOBSCOPE_APOLLO_API_KEY }}
          JOBSCOPE_MUTATIONS_JSON: ${{ inputs.mutations_json }}
        run: |
          python -m jobscope companies seed
          if [ -n "$JOBSCOPE_MUTATIONS_JSON" ]; then
            printf '%s' "$JOBSCOPE_MUTATIONS_JSON" > data/monitoring-actions.json
            python -m jobscope companies apply --actions-file data/monitoring-actions.json
          else
            python -m jobscope companies scan || echo "::warning::one or more company monitors need attention"
            if [ "${{ inputs.full_scan }}" = "true" ]; then
              python -m jobscope scan --mode discovery --force-discovery || echo "::warning::broad discovery failed (non-fatal)"
            else
              python -m jobscope scan --mode discovery || echo "::warning::broad discovery failed (non-fatal)"
            fi
            if [ "${{ inputs.inbox_backfill }}" = "true" ]; then
              python -m jobscope inbox --backfill --include-spam --initiator cloud_refresh
            else
              python -m jobscope inbox --since "$(date -u -d '7 days ago' +%F)" --include-spam --initiator cloud_refresh
            fi
            python -m jobscope inbox --reclassify --initiator cloud_refresh
            python -m jobscope match
            python -m jobscope reviews sync
            python -m jobscope enrich || echo "::warning::public enrichment failed (non-fatal)"
            python -m jobscope new --email || echo "::warning::new-match digest failed (non-fatal)"
            python -m jobscope outreach-scan || echo "::warning::outreach scan failed (non-fatal)"
          fi

      - name: Save encrypted DB back to the `data` branch
        if: steps.guard.outputs.ready == 'true'
        env:
          JOBSCOPE_DB_KEY: ${{ secrets.JOBSCOPE_DB_KEY }}
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          RESTORED_DATA_SHA: ${{ steps.restore.outputs.data_sha }}
        run: |
          set -euo pipefail
          python -m jobscope.core.snapshot data/jobscope.db
          cloud_db="$(mktemp)"
          verify_db="$(mktemp)"
          trap 'rm -f "$cloud_db" "$verify_db"' EXIT
          python -m jobscope.core.snapshot data/jobscope.db --cloud-copy "$cloud_db"
          node scripts/crypt-file.mjs encrypt "$cloud_db" data/jobscope.db.enc
          node scripts/crypt-file.mjs decrypt data/jobscope.db.enc "$verify_db"
          python -m jobscope.core.snapshot "$verify_db"
          cmp --silent "$cloud_db" "$verify_db"

          tmp="$(mktemp -d)"
          cp data/jobscope.db.enc "$tmp/jobscope.db.enc"
          cp data/jobscope.db.restored.enc "$tmp/jobscope.db.previous.enc"
          cd "$tmp"
          git init -q
          git checkout -q -b data
          git add jobscope.db.enc jobscope.db.previous.enc
          git -c user.name="github-actions[bot]" \
              -c user.email="41898282+github-actions[bot]@users.noreply.github.com" \
              commit -q -m "chore: encrypted db snapshot $(date -u +%FT%TZ)"
            git push -q \
              --force-with-lease="refs/heads/data:${RESTORED_DATA_SHA}" \
              "https://x-access-token:${GITHUB_TOKEN}@github.com/${GITHUB_REPOSITORY}.git" \
              data:refs/heads/data

      - name: Install web deps
        if: steps.guard.outputs.ready == 'true'
        run: cd web && npm ci

      - name: Configure git auth for the publish push
        if: steps.guard.outputs.ready == 'true'
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: git config --global url."https://x-access-token:${GITHUB_TOKEN}@github.com/".insteadOf "https://github.com/"

      - name: Publish empty shell + encrypted site to gh-pages
        if: steps.guard.outputs.ready == 'true'
        env:
          PYTHONPATH: "."
          JOBSCOPE_APPS_PASSPHRASE: ${{ secrets.JOBSCOPE_APPS_PASSPHRASE }}
          JOBSCOPE_PUBLISH_FORCE: "1"
        run: bash scripts/publish.sh --encrypted --force

# ============================================================================
# SETUP (one-time)
#
# 1. Repository Secrets  (Settings -> Secrets and variables -> Actions -> New secret):
#      JOBSCOPE_DB_KEY           a long random string -- encrypts your DB on the `data` branch
#      JOBSCOPE_APPS_PASSPHRASE  the same passphrase you unlock the Applications tab with
#      JOBSCOPE_GMAIL_APP_PW     Gmail app-password for account 1
#      JOBSCOPE_GMAIL_APP_PW_2   Gmail app-password for account 2 (omit if you have one account)
#      JOBSCOPE_SMTP_PASSWORD    (optional) SMTP app-password used to email a digest of new
#                                matches each refresh; only sends when your JOBSCOPE_CONFIG sets
#                                email.enabled: true (otherwise the digest step is a no-op)
#      JOBSCOPE_HUNTER_API_KEY   (optional) Hunter.io key -> richer HR contacts for applied
#      JOBSCOPE_APOLLO_API_KEY   (optional) Apollo key. Both unset -> deterministic discovery only.
#      JOBSCOPE_CONFIG           your full config.yaml as text, but with NO passwords in it
#                                (accounts use password_env: JOBSCOPE_GMAIL_APP_PW / _2),
#                                and set:  ai: { enabled: false },  output: { db_path: data/jobscope.db },
#                                inbox.include_spam: true
#
# 2. Seed your history once (so the cloud DB starts from your real data), from your
#    local checkout on `main` -- encrypts data/jobscope.db and force-pushes it to `data`:
#      $env:JOBSCOPE_DB_KEY = "<same value as the JOBSCOPE_DB_KEY secret>"
#      ./scripts/seed-cloud-db.ps1
#
# 3. Trigger a first run manually: Actions -> refresh -> "Run workflow" (also how you
#    scan on demand from your phone via the GitHub mobile app). The cron then keeps it
#    fresh every 3 hours.
# ============================================================================
