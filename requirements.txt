# Core (Phase 1): config, HTTP, RSS
pyyaml==6.0.2
requests==2.33.0
pyjwt[crypto]==2.13.0
feedparser==6.0.11

# Enrichment (Phase 2)
yfinance==0.2.40

# Tailoring + PDF + assisted apply (Phase 3-4)
# playwright is deliberately NOT here -- it is the `pdf` extra. Both users of it
# (tailored-resume PDF export, `apply --assist`) already degrade without it, and
# it also needs a separate ~150MB `playwright install chromium`.
#   pip install "jobscope[pdf]" && python -m playwright install chromium
pypdf==6.14.2
markdown==3.8.1

# Dev / tests
pytest==9.0.3
ruff==0.12.4
detect-secrets==1.5.0
