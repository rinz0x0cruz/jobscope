# Core (Phase 1): scraping, config, HTTP, RSS
python-jobspy>=1.1.80
pyyaml>=6.0
requests>=2.31
feedparser>=6.0

# Enrichment (Phase 2)
yfinance>=0.2.40

# Tailoring + PDF + assisted apply (Phase 3-4)
playwright>=1.40
pypdf>=4.0
markdown>=3.5

# Dev / tests
pytest>=8.0
