# Core (Phase 1): scraping, config, HTTP, RSS
python-jobspy==1.1.80
pyyaml==6.0.2
requests==2.32.3
feedparser==6.0.11

# Enrichment (Phase 2)
yfinance==0.2.40

# Tailoring + PDF + assisted apply (Phase 3-4)
playwright==1.40.0
pypdf==4.0.0
markdown==3.5

# Dev / tests
pytest==8.3.4
