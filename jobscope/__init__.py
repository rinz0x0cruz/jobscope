"""jobscope - resume-driven job scout, enricher, and application-prep tool.

Deterministic-first (the "80% logic"), with optional AI upgrades (the "20%").
Runs fully offline for its core loop; every network/AI/browser feature degrades
gracefully when a dependency or key is missing.
"""

__version__ = "0.1.0"
